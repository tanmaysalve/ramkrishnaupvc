import { UserRound, ShieldCheck } from 'lucide-react'
import { useAuth } from '../../contexts/AuthContext'
export function UsersPage(){const {user}=useAuth();const users=JSON.parse(localStorage.getItem('rk_users')||'[]');if(user?.role!=='ADMIN')return null;return <div><div className="page-heading"><div><p className="eyebrow">ADMINISTRATION</p><h1>Users</h1><p className="muted">Demo users and their assigned application roles.</p></div></div><section className="panel users-panel">{users.map((u:any)=><div className="user-row" key={u.id}><div className="avatar large">{u.displayName.slice(0,1)}</div><div className="user-main"><strong>{u.displayName}</strong><span>@{u.username}</span></div><div className="role-pill">{u.role==='ADMIN'?<ShieldCheck size={15}/>:<UserRound size={15}/>} {u.role==='ADMIN'?'Administrator':'Sales User'}</div><div className="user-status">Active</div></div>)}</section></div>}